package constants;


public class ReadConstants {/*

    //public static String CONFIG_FILE_NAME;

    public static String utils_Prop_File = "Util.properties";

    private static final Logger LOGGER = Logger.getLogger(ReadConstants.class.getName());

    static {
        LOGGER.addHandler(LogHelper.getLogHandler());
        Properties props = new Properties();
        try {
            props.load(new FileInputStream(utils_Prop_File));
           // CONFIG_FILE_NAME = props.getProperty("CONFIG_FILE_NAME");
        } catch (IOException e) {
            e.printStackTrace();
            LOGGER.severe("Problem in loading " + utils_Prop_File + " file");
        }
    }
*/}
